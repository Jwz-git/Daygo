# <module> — <用户功能名称>

> 复制模板后必须用实际内容替换占位项；模块标识与文件名一致。
> 公共执行规则和门禁见 [09](../09-roadmap.md)，字段级契约唯一出处是 [05](../05-interface-contract.md)。

## 用户结果与范围

写明用户完成什么操作、观察什么结果；列需求 ID、非目标及未决产品边界。
功能模块包含所需的 Go、repository、平台接入、绑定、store 与 UI，不改变技术分层。

## 当前状态与证据

分别填写实现进度（未开始 / 部分实现 / 实现齐备）与单元、fake 契约、真实集成、长期观察状态。
列当前代码路径、已运行命令及日期 / commit / 环境。未运行写“未运行”，不得将目标路径当作现状。

## 能力与跨层职责

| 输入能力 / 契约 | 负责模块 | 可独立推进 / fake 可证明什么 | 真实接入前置条件 |
|---|---|---|---|
| <具体能力，不写整个模块必须完成> | <负责人> | <可运行切片> | <验收证据> |

列明本模块输出给其他模块的能力及独立验收条件。
指定各技术层与共用设置、schema、事件、端口的职责；所有 SQL 在 internal/storage，
密钥不进入通用设置。公共能力按 09 §9.3 的负责人协调。

## 实验与失败条件

| 实验 / 风险 | 输入与操作 | 预期结果 | 失败条件 / 证据 |
|---|---|---|---|
| <固定 ID 或既有测试 ID> | <匿名夹具、步骤、环境> | <先固定的可观察结果> | <停止哪项接入、保存什么> |

有风险的行为先写输入和期望，再实现；期望变更必须说明决定。
引用既有 DB / IT / MC 测试，不复制两套相互漂移的规范。真实服务和 OS 实验明确所需环境。

## 实现切片与集成

按本模块内部依赖列出可独立验证的切片：实验 / 决策、夹具与纯逻辑、持久化 / 适配、
绑定与事件、store 与 UI、真实闭环。每个切片写产物和完成标准，不要求其他模块全部结束。
共享边界依次合入。前端写后等事件重拉，空态 / 错误态 / 加载态与 i18n 随功能完成。

## 验收、阻塞与回退

列模块完成条件、公共门禁和真实数据接入证据；说明 fake 的局限及哪些工作仍可继续。
列待决项的负责人和触发时机、接入失败的回退步骤。回退不能靠删除用户数据或丢弃未确认帧。

## 验证记录

| 日期 / commit / 环境 | 命令或人工步骤 / 输入 | 期望与实际结果 | 限制 / 下一步 |
|---|---|---|---|
| <待填写> | <真实执行后记录> | 未运行 | <缺少的证据> |
