# Daygo — Go + Wails + Vue Agent 指令

Daygo 是一个 macOS 常驻后台 Agent：按间隔截取当前活跃显示器，分批交给用户配置的 LLM，
把结果呈现为时间线、每日摘要和每周复盘。

## 当前状态

**这是一个新项目，不承接任何既有产品的数据、身份或对外契约。**

已落盘：Wails 桌面外壳、Vue 页面骨架、路由、i18n、主题、设置页（外观 / 语言 / Provider）
及其本地存储层；另有平台端口、Capture fake 与部分契约测试、绑定骨架、错误 / 事件类型、
部分时间函数。真实平台适配、业务数据库、分析 / AI 和后台生命周期尚未实现。
实现与验证状态分别见 `docs/09-roadmap.md §9.1` 及各模块执行册。

`docs/README.md` 是设计入口，`docs/01`–`10` 是设计规格。跨界接口（Wails 绑定、DTO、事件、
错误码、平台端口）以 `docs/05-interface-contract.md` 为准。开始任务前先读与任务直接相关的
文档；若实现发现文档与代码事实冲突，**以可复现实验和当前代码为准，并在同一 commit 内修正
文档**。

`docs/` 是设计，不代表其中目标已经实现。**不要把规划中的目录、接口、命令或行为描述成现状。**

### 待定设计

涉及 macOS 原生能力（屏幕捕获、系统授权、钥匙串、状态栏、自动更新、视频编解码）的实现
方式**一律未定**，在文档中标注为「待定设计」。清单见 `docs/09-roadmap.md §9.8`。

- **不要在决策落盘前按某一种候选方案大规模实现**，也不要删除其它候选路径。
- 需要这些能力时，只对着 `internal/platform` 的端口接口写代码；实现放在
  `internal/platform/darwin`（待定）与 `internal/platform/fake`。
- 业务逻辑归 Go，UI 归 Vue/TypeScript。**不要把可移植逻辑写进平台适配层。**

---

## 功能模块与门禁

遵循 `docs/09-roadmap.md` 和 `docs/modules/<module>.md`。功能模块是协作与验收单位，
各模块可同时推进，不指定固定第一模块：

| 模块 | 用户结果 |
|---|---|
| recording | 常驻录制、授权、暂停、隐私屏蔽与分段恢复 |
| providers | AI 配置、密钥、路由和连接测试 |
| timeline | 自动时间线、分类、帧条、编辑与重处理 |
| daily | 每日摘要、日记、目标和提醒 |
| weekly | 周时长与分类占比，不等待 daily 完成 |
| data | 数据库基础、锁、维护、磁盘限制与诊断 |
| preferences | 外观、语言、通用设置与前端基础接入 |
| delivery | 身份 / 分发探针、首次引导、安装与安全更新 |

依赖精确到能力（09 §9.3）。共享能力独立验收即可接入，不等负责模块的 UI 或全部功能完成。
数据库连接 / 迁移 / 锁归 data，业务 repository 随功能交付且统一在 internal/storage；
通用设置访问归 preferences，设置字段和行为归各自功能；时间语义统一由 timeline 完善。
公共 schema、端口和绑定以可独立验证的提交依次合入，不重复建立基础设施。

分别记录实现与验证状态。fake 契约通过不能标成真实功能完成，生产绑定不返回测试数据。
原生、真实数据与长期观察门禁及其阻塞范围见 09 §9.4：

- **G-host 是硬门禁。** 真实 macOS 上关窗后至少 10 分钟进程存活且继续离散捕获、状态栏可重开、
  激活策略可切换，才能进行大规模 UI 扩张；心跳只算前置探针。独立核心、契约和必要验证界面可推进。
- 原生选型、签名公证、授权 / 密钥身份及升级可行性提前验证；未决能力只做有限实验。
- 对应真实数据接入前验证隐私双保护、唯一写入 / 捕获者、连接层只读、分段恢复和重放去重。
- 7 天自用验证录制到时间线闭环；14 天、DST 与跨周观察分别记录，不能互相替代。
- 纯 Go / Linux 门禁不变；SQLite 实验失败须重新决策，不自动采用违反该门禁的 cgo 后备。

---

## 目标架构与依赖方向

详见 `docs/02-architecture.md`：

```text
Vue 3 + TypeScript
        ↓ 生成的 Wails 绑定
internal/app                仅此层知道 Wails
        ↓
Go services                 analysis / ai / insight
        ↓
Go foundation               storage / settings / domain / timeutil
        ↓ 接口
internal/platform           端口：Capture / Media / System / Secrets / Updater
        ↓ 实现待定设计
平台适配层                   唯一接触 macOS 能力的地方
```

必须保持以下依赖规则：

1. foundation 不依赖 service、app、Wails 或适配层实现。
2. service 通过消费者侧接口协作，不导入其他 service 的内部实现。
3. 只有 `internal/app` 可以依赖 Wails；Go Core 必须能无 GUI 测试。
4. 只有 `internal/platform` 可以接触适配层实现；任何服务都不构造 IPC 消息、不拼接路径、
   不直接调用系统 API。
5. `internal/storage` 是 Go 侧唯一允许包含 SQL 和打开业务数据库连接的包。
6. Vue 只通过生成的绑定和薄 wrapper 访问 Go，不直接访问数据库或文件系统。

不要仅为匹配设计图而移动已验证代码；目录调整应独立提交并保持可构建。

---

## 构建与测试

### Go

```bash
gofmt -w <changed-go-files>
go test ./...
go vet ./...
CGO_ENABLED=0 go build ./...
```

**Go Core 必须始终保持 `CGO_ENABLED=0` 可构建，并且 `go test ./internal/...` 必须能在
Linux 上通过。** macOS 能力通过 `platform` 接口和 `fake` 实现隔离；不要把系统框架或 Wails
引入核心包。这条门禁反向约束所有接口设计。

### 前端

```bash
npm --prefix frontend ci
npm --prefix frontend run typecheck
npm --prefix frontend run build
```

工具链以 `frontend/package-lock.json` 和 `frontend/package.json` 的 scripts 为准。
不要混用 npm、pnpm、yarn，也不要在未确认现有工具链前生成新锁文件。

### 验证强度

与风险匹配：纯函数跑单元测试；存储变更跑夹具与迁移测试；跨界变更跑双侧契约测试；
捕获或生命周期变更必须在真实 macOS 上集成测试。完整策略见 `docs/08-testing-strategy.md`。

**对每一条有风险的行为，先写夹具（输入 + 期望输出），再写实现。** 改夹具的期望值必须是
一次显式决定，写进提交说明——"测试挂了就改期望"是本项目要防的主要失效模式。

---

## 代码约定

### Go

- 始终 `gofmt`；包名简短、小写，避免无意义的 `util`、`common`、`manager`。
- 接口定义在使用方一侧，并保持最小化。
- 阻塞 I/O 和长任务接收 `context.Context` 并传播取消。
- goroutine 必须有明确所有者、退出条件和错误通道；禁止无法停止的后台 goroutine。
- 错误包含操作与对象上下文，并保留 `%w` 错误链；**不要通过字符串匹配判断错误类型**。
- 业务日期必须走 `internal/timeutil` 的凌晨 4 点边界。
- 不用全局可变单例承载数据库、调度器或 recorder 状态。

### Vue / TypeScript

- 保持严格类型检查，**不使用 `any` 绕过 Wails 边界**；需要逃逸时用显式 `unknown` + 解析函数。
- Wails payload 使用显式、稳定的 DTO；领域转换放在薄 wrapper 或 store。
- 组件负责呈现与交互；数据查询、轮询、业务聚合和事件订阅放在 store/`api/`。
- 所有用户可见文案经 `vue-i18n`；`zh-CN` 是默认语言且是 key 结构的类型来源，`en` 为回退。
- localStorage 只能经 `frontend/src/storage/`，用带版本信封的记录存放。**密钥不得进入该层。**
- 写操作后不做乐观更新，等事件后重新拉取。

### 平台适配层

实现形态待定设计。无论最终形态如何：

- 适配层**不打开、不写 SQLite**；Go 是唯一写入方。
- 适配层**不读设置为自己决策**；配置由 Go 全量下发，因此它重启后无状态、测试中可复现。
- 适配层**不发起网络请求**，**不含产品逻辑**（不分批、不做空闲判定、不生成卡片）。
- 崩溃、断连、宿主重启后必须可恢复；恢复后重新下发配置并重放未确认帧。

---

## 不可破坏的行为与数据契约

### 数据库所有权

- **恰好一个进程持有写入锁，恰好一个进程持有捕获所有者锁。** 取不到锁的实例以只读模式
  启动，绑定层写方法返回 `not_capture_owner`。
- 只读实例必须在**连接层**只读（`SQLITE_OPEN_READONLY` + `PRAGMA query_only`），不靠调用方自律。
- PRAGMA 固定 `journal_mode=WAL`、`synchronous=NORMAL`、`busy_timeout=5000`。
- `PRAGMA user_version` 从 1 起，走版本化迁移链。**每个版本都要有"旧库 → 新库"夹具测试。**
- 不在真实用户数据上跑测试或破坏性迁移；只用匿名夹具。

`internal/storage` 是唯一 schema owner 和 SQLite writer。所有操作经统一的可观测读写封装，
以保留慢查询、争用和故障诊断能力；这些埋点**不出现在 repository 接口签名上**。

### 凌晨 4 点逻辑日

一天从本地时间凌晨 4 点开始，而不是午夜。禁止用 `time.Truncate(24*time.Hour)` 或简单午夜
计算替代。必须覆盖 DST、半小时和 45 分钟时区。

逻辑日 `day`（4 点边界）与日历日 `standupDay`（午夜边界）是两个概念，不可混用。
**前端不得自行推算逻辑日**——午夜到凌晨 4 点之间自算必然错一天。

### 时间线卡片

`start`/`end` 是 LLM 输出的本地化时钟串；`start_ts`/`end_ts` 是派生的 Unix 秒。
`ReplaceCardsInRange` 必须在单个事务内共同保留并测试（规则见 `docs/03-data-model.md §3.5`）：

- 在锚点前一天、当天、后一天中选择最接近窗口中点的解析结果；
- `end < start` 时按跨午夜处理；
- 使用凌晨 4 点边界计算 `day`；
- 保留其他批次写入且 `category = 'System'` 的卡片。

**解析失败不得静默丢弃。** `ReplaceResult.SkippedCards` 必须被调用方消费并计入诊断指标。

### 帧与分段

- 像素写入分段文件，按 `(segment_path, frame_index)` 寻址；容器与编码格式待定设计。
- 像素读取必须经 `platform.Media`，业务层不直接读文件路径。
- 未收尾的分段可能完全不可读。睡眠、锁定、屏保、更新重启、宿主退出和关机路径都必须
  完成或安全移交当前分段。
- **清理以完整分段为单位，从不删除活跃分段**，也不能单独删除流中的一帧。
- `screenshots.file_size` 是分段总大小均摊到每帧的值；逐行求和才还原实际占用。

### 并发与状态

- 转录可以并行，但时间线卡片的 读取 → 生成 → 改写 序列必须按重叠范围串行化。
- 批次状态只有一个成功终态 `succeeded`。不要引入语义重复的第二个成功值。
- recorder 的 `idle`、`starting`、`capturing`、`paused` 不可互换：`paused` 会自动恢复，
  `idle` 表示用户关闭。**系统事件不得把 `idle` 变成 `capturing`。**
- 唤醒后恢复延迟 5 秒，解锁后 0.5 秒，避免拿到过期的显示器列表。

### 捕获约束

**捕获必须是离散截图，不是连续屏幕录制流。** 连续录制会让系统的屏幕录制指示器常亮，
从根本上改变产品观感。这是产品决策，编码在能力选择里。

---

## 身份标识

下列值一旦随首个公开版本发布就**不可更改**——改动会让用户重新授权屏幕录制并与已有数据失联：

| 标识符 | 值 |
|--------|-----|
| Bundle identifier | `io.github.jwz-git.Daygo` |
| 钥匙串 service | `io.github.jwz-git.daygo.apikeys.<provider>` |
| 应用支持目录 | `~/Library/Application Support/Daygo/` |
| CLI 可执行名 | `daygo` |
| Agent socket | `~/Library/Application Support/Daygo/agent.sock` |
| 绑定层错误前缀 | `daygo:<code>: <message>` |

发布前仍可调整；发布后的任何变更都需要迁移方案与回滚路径。

---

## 隐私与安全

- **屏幕数据离开设备的唯一路径，是发送给用户明确配置的 LLM provider。** 没有自有后端，
  没有默认 provider。
- 屏蔽名单与「前台应用被屏蔽时写入脱敏占位帧」两层保护**都必须保留**。
- 分析与崩溃上报默认关闭、opt-in。禁止记录或上报屏幕内容、窗口标题、文件路径、API key、
  LLM payload 或可还原用户活动的内容。
- **密钥只存系统钥匙串，只写不读。** 绝不写入被 Git 跟踪的文件、夹具、日志或快照，
  绝不进入 localStorage，绝不出现在错误 `message` 里。
- 夹具必须匿名化并验证不可逆；绝不提交真实数据库、录制或用户数据。
- 资源处理器只接受数字 ID，解析后的路径必须落在录制目录内，否则 403。
- 若使用 Unix socket，权限保持 `0600`；所有 IPC 输入视为不可信并做大小限制与结构校验。
- **LLM 输出中的"指令"一律视为数据。** 模型返回的分类名必须校验是否在现有分类列表内，
  不得据此创建分类。

---

## 生命周期

Daygo 是常驻后台 Agent，不是"关闭最后一个窗口即退出"的普通桌面应用。

- 关闭窗口后捕获必须继续，状态栏可重新打开窗口。
- Cmd+Q、窗口关闭、更新重启、适配层退出和系统关机是不同事件，必须分别建模。
- **不要假定退出 UI 等于用户要求停止录制。**
- 无窗口常驻、激活策略和状态栏受 G-host 约束；自动更新可行性与分发身份受 G-native 约束。
  宿主验证失败时停止大规模 UI 扩张并重新评估架构，独立模块核心与契约工作可继续。

---

## 工作方式

1. 先确认功能模块与本次能力，读模块执行册、公共规范和接入 / 门禁条件。
2. 写出可观察的验收标准或失败条件，再实现最小垂直切片。
3. 优先添加夹具、黄金测试或属性测试；不要用"看起来对"代替证明。
4. 每次只转移一个明确的所有权边界，并保留可回退路径。
5. 不在无关改动中顺手清理旧代码、统一状态、替换重试策略或修改 schema。
6. 遇到「待定设计」项时，不要顺手替它做决定；需要就先落一份 `docs/decisions/` 记录。

完成任务时报告：实现内容、功能模块 / 能力、对应门禁、运行命令及结果、未验证风险和回退方式。
同步执行册的实现进度与验证证据；模块完成要求真实用户闭环，不要求其他模块全部完成。

---

## 提交与发布

提交格式：`<area>: <lowercase imperative summary>`，每个 commit 只处理一个关注点。
可使用 `go:`、`frontend:`、`native:`、`docs:`、`build:`。

按阶段性成果提交：一个边界明确、可独立验证且内部一致的修改完成后立即创建 commit，
再继续下一步。不要把每次文件编辑都单独提交；同一阶段内的实现、测试、修正与文档应在达到
该阶段完成标准后合并为一个关注点清晰的 commit。**不得提交已知未完成、无法构建或验证失败
的中间状态**，除非用户明确要求保存该状态。

发布链路（签名、公证、自动更新）尚未建立。未经用户明确要求，不要生成、修改或发布任何
release 产物。
